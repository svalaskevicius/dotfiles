#!/bin/bash

D="$(dirname "$0")"

printf "Reset root snapshot? (y/N)\n"
read reset
if [ "$reset" = "y" ]; then
    sudo "$D/reset-root-snapshot"
fi

sudo "$D/pacnanny" -Syu

paru

sudo pacdiff

printf "Press ENTER to exit\n"
read 
